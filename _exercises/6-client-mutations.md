# 6 – Client Mutations in React

## Task 1 – Implement createTweet mutation

Go to `src/mutation.js` to add the corressponding mutations.

Go to the `pages/Home` component set corresponding mutation options:

- [ ] Pass the corresponding variables to the mutation
- [ ] Reset the input value to empty string after the mutation
- [ ] Refetch queries (might need variables): `allTweetsQuery`, `userQuery`

Go to the `pages/Home` component to set all corresponding mutation options.

> 💡 Check the Apollo docs to learn more about mutations:
> https://www.apollographql.com/docs/react/data/mutations

## Task 2 – Implement deleteTweet mutation

Go to `src/mutation.js` to add the corressponding mutations.

Go to the `components/Tweet` component set corresponding mutation options:

- [ ] Pass the corresponding variables to the mutation
- [ ] Refetch queries (might need variables): `userQuery`

> 💡 Check the Apollo docs to learn more about mutations:
> https://www.apollographql.com/docs/react/data/mutations

## Task 3 – Implement updateUser mutation

Go to `src/mutation.js` to add the corressponding mutations.

Go to the `components/ProfileForm` component set corresponding mutation options:

- [ ] Pass the corresponding variables to the mutation
- [ ] Reset the input values after the mutation
- [ ] Refetch queries (might need variables): `allTweetsQuery`, `userQuery`

> 💡 Check the Apollo docs to learn more about mutations:
> https://www.apollographql.com/docs/react/data/mutations
